<section class="full-width-image">
    <div class="container">
        {% if fullImage.src %}
            <div class="image-wrapper">
                <img src="{{fullImage.src}}" alt="{{fullImage.alt}}" />
            </div>
        {% endif %}
    </div>
</section>

