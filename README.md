# Eleventy Notes

`addPassthroughCopy` make assets available in the root directory.

# Local host setup 

Use the latest version of node. 

Run `npm run dev` to see local host running @ `http://localhost:8080/`








